<Window x:Class="SupperChat.MVVM.View.CreateGroupWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="创建群聊" Height="250" Width="400"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="群聊名称：" FontSize="14" Margin="0,0,0,10"/>
        <TextBox Grid.Row="1" Height="30" FontSize="14" 
                 Text="{Binding GroupName, UpdateSourceTrigger=PropertyChanged}" 
                 Margin="0,0,0,20"/>

        <Button Grid.Row="2" Content="创建" Width="80" Height="30"
                HorizontalAlignment="Right" Click="CreateButton_Click"/>
    </Grid>
</Window>
